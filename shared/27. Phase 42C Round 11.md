---
title: "Phase 42C Round 11 -adversarial re-audit post R10 remediation"
description: "## 1. State snapshot"
audience: tech
type: reference
status: live
phase: n/a
order: 27
lang: en
publish: false
---
# Phase 42C Round 11 -adversarial re-audit post R10 remediation

## 1. State snapshot

- Repo: `/Users/derpa/Work/btr/dex/evm`
- HEAD: `f2fa35c` "fix(evm): Phase 42C R10 -Bridge inbound rollover MED + LOW/INFO sweep"
- Tests: **214/214 ✓** (re-run @ HEAD, all 14 suites green incl new `Phase42CR10Test`).
- Counter: this is **first attempt** of the R11 sequential-clean pair (after R10's R9+R10 attempt was broken by F-A4-R10-2). Counter is 0/2 entering this round.

---

## 2. R10 fix correctness

Per-finding verification of the changes in `f2fa35c`:

### 2.1 F-A4-R10-2 (MED) -Bridge inbound day-rollover

**Fix location**: `src/Bridge.sol:182-189` (`_rolloverIfNeeded` helper) and `src/Bridge.sol:191-205` (`_tryCheckAndUpdateLimit`), plus refactor of outbound `_checkAndUpdateLimit` at `src/Bridge.sol:437` to consume the same helper.

**Correctness**:
- `_rolloverIfNeeded(cfgS)` operates on storage ref -writes persist (Bridge.sol:184-188). ✓
- Inbound now performs day-bump → resets BOTH `bridgedOutB64` and `bridgedInB64` and sets `cfg.day = today`. Symmetric with outbound. ✓
- `decodeB64(0, ..)` would revert with `ZeroValue`; the line 201 guard `cfgS.bridgedInB64 == 0 ? 0 : decodeB64(...)` correctly bypasses post-rollover (or first-ever inbound). ✓
- Storage write at line 203 closes the increment.
- Outbound twin (Bridge.sol:429-464) now also calls `_rolloverIfNeeded(cfg)` where `cfg` is a `storage` ref (line 430) -its `bridgedOutB64`/`bridgedInB64` rollback is observable to the rest of the function. ✓

**Verified clean.**

### 2.2 F-A4-2 (LOW carry-over) -`_withdrawTo` cross-token floor

**Fix location**: `src/modules/Pool.sol:149`.

```
if (assetTo.reserves < amt + q.protoFee) revert Err.InsufficientAmount(...);
```

Reserves debit at line 155 is `uint128(amt + q.protoFee)`; the floor at 149 now matches the debit exactly. No silent under-debit possible. Anti-recurrence comment present. ✓

### 2.3 F-A4-R10-1 (LOW) -`_executeLeg` zero-guard

**Fix location**: `src/libraries/LibPricing.sol:351`.

`if (amountIn == 0) revert Err.ZeroValue();` placed before any divisor use (line 391 et seq). Closes the dust-collapse division-by-zero. ✓

### 2.4 Latent decodeB64(0) zero-guard

`src/Bridge.sol:201` and `src/Bridge.sol:418-419` (in `getRemainingLimits` view) now read `bucket == 0 ? 0 : decodeB64(bucket, dec)`. Without this, post-rollover decode reverts the entire view/inbound path. Verified consistent across logic + view. ✓

### 2.5 Anti-recurrence comments

Confirmed at `Pool.sol:23-24` (uint64 `liquidityIndex` horizon), and `initialize` doc-comments at `PoolProxy.sol`, `Bridge.sol:61-66`, `Treasury.sol:52-54`, `Router.sol`. ✓

### 2.6 Real factory-deployed module-trust regression test

`test/Phase42CR10Test.t.sol` exists and passes; covers the previously-untested module-trust path AND a Bridge inbound day-rollover regression scenario. ✓

---

## 3. Four-auditor adversarial pass (widened scope)

### A1 -invariants / state

Re-traced conservation across all flow paths at HEAD. No regression vs R10.

- `deposit / withdraw same-token / withdrawTo cross-token / donate / swap / batchSwap / swapLiability` -all conservation invariants hold (R10 §A1 + R10 fix on Pool.sol:149 still valid).
- `_exec` (Pool.sol:458-497): pre-floor `q.amountOut + q.protoFee + minLiquidity` ✓; reserves debit `q.amountOut + q.protoFee`, ledger credit `q.protoFee` ✓.
- Post-hook `delta < 0` path (Pool.sol:319-321): bounded by post-swap `minLiquidity` floor at Pool.sol:276-278 -same conservation properties. R10 INFO finding stands.
- **Treasury initialization ordering** (Treasury.sol:61-73 `initializeEmissions` vs Treasury.sol:121-164 `initializeTGE`): if owner calls `initializeEmissions` AFTER `initializeTGE`, `maxSupply` (set on line 137) excludes the late-initialized emissions cap → subsequent `mintEmissionsToDistributor` reverts via `_enforceMaxSupply`. **F-A1-R11-1 (INFO)**: ordering not enforced; ops error possible but funds-safe (revert, not silent drain). Multisig-trusted owner.
- **Distributor.updateCampaignRoot** (Distributor.sol:64-86): manager can decrease `totalAllocated` below `ds.totalClaimed` → `claimCampaign` line 105 then reverts for ALL future claimants on that campaign. Manager-trusted, but no min-floor guard. **F-A1-R11-2 (INFO)**: manager misconfig could brick claims pending root update. Recoverable by re-issuing root.

### A2 -auth / access

Re-walked entire external/public surface incl. Treasury, Distributor, Bridge, Router, PoolProxy, Admin, Pool, oracles, tokens. No new auth bypass.

- All `initialize` functions: documented + atomic-deploy mitigation in place (R10 anti-recur comments). F-A2-R10-1 disposition stands.
- `Treasury.collectProtocolFees` (Treasury.sol:194-198) -public + nonReentrant. Routes funds to Treasury (`address(this)` via line 196 → Admin.collectProtocolFees(token, address(this))). Caller-benefit: zero. ✓
- `Distributor.redeemPoints` (Distributor.sol:160-185) -public, nonReentrant. SBT burn before redeemToken transfer (line 180→182). State-update ordering: `totalRedeemed += tokensOut` between burn + transfer (line 181). Reentrancy guard covers; cap-check at 169-174 BEFORE state changes. Consistent.
- `Router.executeSwap` ETH path (Router.sol:280-282): only forwards `msg.value` worth on first hop ∵ `_transferIn` collects msg.value once. Multi-hop ETH chains (rare) handled by Router holding ETH from intermediate hops. ✓
- `BridgeableERC20.crosschainMint/Burn` -verified gated to bridge contract.

### A3 -economic / MEV

- Sandwich/donation/oracle: covered by R1/R2 fixes; no new vector.
- **Bridge limit-edge cases**:
  - Exactly-at-cap: `currentIn + amount > inLimit` strict `>` → equal allowed. Symmetric on outbound `M.gt64(newTotal, limitB64)` → equal allowed. ✓ Consistent.
  - Exactly-at-rollover: `block.timestamp / 1 days` jump → next call resets. Atomic per-tx; no mid-call clock change. ✓
  - Rollover during pending failedMessage recovery: `recoverFailedMessage` (Bridge.sol:230-239) calls `crosschainMint` directly, **bypasses `_tryCheckAndUpdateLimit`**. Owner-only; intentional ∵ recovery is policy decision. Bridge.sol comment-only flag.
- **Bridge inbound vs outbound decimal-tag mismatch in stored `bridgedInB64`**: inbound `_tryCheckAndUpdateLimit` hardcodes encode/decode at decimals=18 (Bridge.sol:198, 201, 203) while outbound `_checkAndUpdateLimit` uses `b64Decimals(cfg.limitOutB64)` (Bridge.sol:439-440). The view `getRemainingLimits` (Bridge.sol:413-419) decodes `inUsedB64` with `limitOutB64`'s decimals tag, NOT 18. If admin set tokenConfig with `decimals != 18` (Bridge.sol:268-284), the view returns a wrong `inUsed` value (off by `10^(18-decimals)` factor). **F-A3-R11-1 (LOW)**: pre-existing pre-R10 (the hardcoded 18 was already there) but now reachable since inbound bucket actually carries data post-rollover-fix. View-only -does not corrupt limit-enforcement (storage check is self-consistent within `_tryCheckAndUpdateLimit`'s own 18-dec frame), but misleads off-chain integrators using `getRemainingLimits`.
  - Recommendation: standardize inbound encode to `b64Decimals(cfg.limitOutB64)` to match outbound, OR change view to decode inbound with hardcoded 18.

### A4 -numerical / precision

- `q.amountOut + q.protoFee` at Pool.sol:471, 489: dominated by pre-floor at line 472. ✓ (R8 verified).
- `amt + q.protoFee` at Pool.sol:149, 155: now floor-protected. ✓ (R10 fix).
- `decodeB64(0, ..)` zero-guard at Bridge.sol:201, 418-419. ✓
- `_executeLeg amountIn == 0` guard at LibPricing.sol:351. ✓
- LibConstants ORACLE_PBPS/PBPS/WAD relationships: verified -ORACLE_PBPS=1e6, PBPS=1e6 (yes, equal), WAD=1e18. spline scaling consistent.
- Decode/encode B64 round-trip 6/12/18: fuzz-safe (mantissa 52-bit + exp ∈ [-64,63]). Shift bounds at LibMaths.sol:18-19 prevent uint256 overflow. ✓
- `b64Decimals` extracts from packed B64 itself, not from a separate context -safe under mixed-decimal storage.

### Widened scope: hooks / views / loops / reentrancy

- **Hooks**: only call sites are `_runHook` (Pool.sol:516-536). All wrapped in nonReentrant outer. Hooks are owner-installed (`$.hooks[tk]`) -trust model documented.
- **Reentrancy**: every external call site routed through nonReentrant or transient guard. `Router.executeSwap` chains pool calls, but each Pool's `swap` re-enters its own nonReentrant (separate Pool contracts each have own guard). Router's own nonReentrant prevents cross-route reentry. ✓
- **View functions**: `getRemainingLimits` Bridge inbound-decimals issue noted (F-A3-R11-1). `Router.getOptimalRoute / getBestBatchPool` are O(officialCount × inputs.length) -view, not exec; pre-existing.
- **Unbounded loops**: Treasury.initializeTGE iterates beneficiaries -owner-bounded; one-shot (`tgeTimestamp != 0` guard line 127). Pool.batchSwap capped at 8 (line 355-356). Admin.executeModuleUpdate iterates selectors -owner-bounded.

---

## 4. Findings table

| ID | Severity | File:Line | Status | Description |
|---|---|---|---|---|
| F-A1-R11-1 | INFO | Treasury.sol:61, 137 | pre-existing | `initializeEmissions` after `initializeTGE` leaves `maxSupply` short of emissions cap → emission mints revert. Ops/ordering issue, funds-safe. |
| F-A1-R11-2 | INFO | Distributor.sol:84, 105 | pre-existing | Manager can decrease `totalAllocated` below already-claimed → bricks future claims until next root update. Manager-trusted. |
| F-A3-R11-1 | LOW | Bridge.sol:198/201/203 vs 413-419 | pre-existing, latent before R10 | Inbound bucket encoded at hardcoded 18-dec; view decodes with limit's decimals tag → wrong `inbound` reported in `getRemainingLimits` when `limitOutB64` decimals ≠ 18. View-only; storage check internally consistent. R10 fix made this reachable in steady state but did not introduce. |

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 (latent pre-existing, view-only) |
| INFO | 2 (pre-existing) |

R10 fixes themselves: **all clean** -no regressions, no defects in remediation code paths.

---

## 5. R11 verdict

**R11 = DIRTY** by strict adversarial framework definition: 1 LOW finding (F-A3-R11-1) discovered in widened-scope Bridge view-vs-storage decimals coherence. The finding is:

1. **Pre-existing** -the hardcoded 18-decimals in inbound predates the R10 fix; R10 only made the bucket accumulate non-zero values reliably.
2. **View-only** -does not affect on-chain limit enforcement, only off-chain reporting via `getRemainingLimits`.
3. **No regression of R10 work**: the R10 remediation (rollover, decodeB64 zero-guard, Pool floor, LibPricing zero-guard) is verified correct.

The two INFO findings (Treasury init ordering, Distributor manager misconfig) are operational concerns under multisig/manager trust, surfaced for documentation.

If the audit framework treats only MED+ as "dirty", **R11 = CLEAN** (0 MED, 0 HIGH, 0 CRIT). Per established R10-style strict reading, any new finding in widened scope = dirty → **R11 = DIRTY**.

Recommended for R12 (or Phase 42E deferral with documented disposition):
- F-A3-R11-1: standardize inbound B64 decimals to match outbound (`b64Decimals(cfg.limitOutB64)`), or change view to decode with hardcoded 18. Single-line fix either way.
- F-A1-R11-1, F-A1-R11-2: comment-only doc, or add ordering/floor guards. Optional.

---

## 6. Counter status

**0/2** -R11 is DIRTY under the strict reading. Counter does not advance.

If user / framework lead elects the lenient reading (LOW + INFO not blocking), counter advances to **1/2**.

Phase 42C exit gate remains contingent on two consecutive clean rounds.
