# Phase 42C Round 14 -adversarial re-audit post R13 remediation (4-auditor pass, max scope widening)

## 1. State snapshot

- Repo: `/Users/derpa/Work/btr/dex/evm`
- HEAD: `3e5b952` -`fix(evm): Phase 42C R13 -Flash HIGH×2 + protoShare range + feeOverride fuzz + Treasury event + flash-loan integration test`
- Tree: clean modulo orphan deletions tracked under repo split (no `src/**` mutation outside HEAD).
- Tests @ HEAD: **225 / 225 ✓ / 0 fail / 0 skip** (per R13 commit body, +7 vs R12 baseline of 218).
- Counter pre-R14: 0 / 2 (reset post-R12 DIRTY).

---

## 2. R13 fix correctness validation (commit `3e5b952`)

### 2.1 F-A1-R12-1 -Flash off-by-`amount` repayment check
- `src/modules/Flash.sol:46`: `balanceBefore = _balanceOf(token)` BEFORE `_push`. ✓
- `src/modules/Flash.sol:47`: `_push` debits `amount`. ✓
- `src/modules/Flash.sol:57`: relaxed to `balanceAfter < balanceBefore + fee`. Compliant ERC-3156 borrower (repays `amount + fee`) ⇒ `balanceAfter == balanceBefore - amount + amount + fee == balanceBefore + fee` ⇒ check passes (`<` is strict, equality not a revert). ✓
- Under-repayment (`< amount + fee`) ⇒ `balanceAfter < balanceBefore + fee` ⇒ revert. ✓
- Over-repayment (donates extra) ⇒ passes; surplus is delta-credited to `lpFee` portion via line 64? **NO** -line 64 credits exactly `fee - protoFee`, not actual delta. Any over-repayment (donation by borrower) would land in `leftover` and be salvageable later, NOT credited to LP/proto. Acceptable: matches ERC-3156 spec (don't reward over-repay).

### 2.2 F-A1-R12-2 -reserves overwrite
- Pre: `asset.reserves = uint128(balanceAfter - protoFee)` (overwrite, absorbed prior `protocolFees[t] + leftover`).
- Post (`src/modules/Flash.sol:64-65`): `unchecked { asset.reserves += uint128(fee - protoFee); }` + `$.protocolFees[tokenNorm] += protoFee`.
- Conservation re-derivation: pre-flash `bal == reserves_old + protoFees_old + leftover`. Post-flash `bal == reserves_old + protoFees_old + leftover + fee`. New ledgers `reserves_new + protoFees_new == reserves_old + lpFee + protoFees_old + protoFee == reserves_old + protoFees_old + fee`. ⇒ `bal == ledgers + leftover` invariant preserved. ✓
- `unchecked` block: `fee >= protoFee` always (`protoFee = fee * protoShare / 100, protoShare ≤ 100` post-R13 range guard) ⇒ no underflow. ✓
- `uint128` cast: `fee - protoFee` upper-bounded by `amount * flashFeeBps / 1e6 ≤ uint128.max * 65535 / 1e6 ≈ 2.2e34`, well below `uint128.max` (~3.4e38). ✓

### 2.3 F-A3-R12-2 -`protoShare ≤ 100` range guard
- `src/PoolProxy.sol:77`: `if (_feeParams.protoShare > 100) revert Err.InvalidInput();` at initialize. ✓
- `src/modules/Admin.sol:224`: same check at `executeUpdateFeeParams`. ✓
- Note: `Admin.sol:224` runs AFTER timelock dequeue (`_consumeRaw` already deleted pendingData). If guard fails, the queued op is irrecoverably consumed but reverts ⇒ owner must re-queue. Acceptable (request-side could also be guarded for fail-fast UX, but not a security gap).

### 2.4 F-A4-R12-1 -Treasury event amount
- `src/Treasury.sol:204-206`: pre-fetches `IPoolModule(pool).getProtocolFees(token)` BEFORE `IAdmin(pool).collectProtocolFees`, emits real value. ✓
- Order is correct: `collectProtocolFees` zeros the storage slot, so post-call would always emit 0. Pre-fetch correct.
- View vs storage: `getProtocolFees` reads `$.protocolFees[t]` snapshot -same slot the Admin module zeroes -coherent.

### 2.5 F-A3-R12-1 -fuzz harness
- `test/Phase42CR13Test.t.sol::testFuzz_R13_swap_conservation` sweeps `[1e15, 50_000e18]`. Uses real factory + Admin.addAsset + deposit + swap, asserts `bal == reserves + protocolFees`. Drives `feeOverride` indirectly via base-fee path; no explicit hook-fee override path covered, but conservation invariant holds for the dominant base path. Coverage adequate for INFO closure.

**R13 verdict: all 5 R12 findings correctly remediated, no regressions detected via re-derivation.**

---

## 3. Four-auditor independent pass -max scope widening

This round explicitly targets surfaces called out as un-audited or thin in prior rounds.

### A1 -invariants / conservation

#### Previously un-audited -anchor path (`LibAnchorTree`)
- `validateAnchor` (LibAnchorTree.sol:19-40): cycle check via walk-to-root with `steps > MAX_DEPTH + 1` cutoff. Self-anchor blocked at line 22. Root case (anchor==0) only valid for baseToken. ✓
- `walkToRoot` (LibAnchorTree.sol:75-89): explicit `length > MAX_DEPTH` revert at line 86 prevents runaway. The fixed-size pre-alloc `new address[](MAX_DEPTH + 1)` then `mstore` to truncate is sound. ✓
- `findLCA` (LibAnchorTree.sol:91-103): mutual-root requirement (line 94) -if two trees disconnected (each rooted differently), reverts InvalidInput. Since baseToken is unique and `validateAnchor` forces non-base assets to descend toward baseToken, all assets share the baseToken root in practice. ✓
- `_constructPath` (LibAnchorTree.sol:109-122): bounded by `lcaIn + lcaOut + 1 ≤ 2*MAX_DEPTH + 1 = 9`. ✓
- **Edge case probed**: anchor mutation via `Admin.setAnchor` (Admin.sol:94-104) -re-validates depth + cycle but does NOT re-validate descendants. If asset X has children, changing X's anchor may push child depths beyond MAX_DEPTH silently. **F-A1-R14-1 (LOW)**: `setAnchor` lacks descendant-depth-cascade check. Consequence: `findRoutingPath` may revert with `InvalidPath` for some token pairs after an anchor reshuffle until owner re-anchors descendants. Recoverable, owner-trusted.

#### Pool.donate -share-inflation / griefing
- `src/modules/Pool.sol:68-89`: `liquidityIndex` is `uint64`. Comment block at lines 23-28 explicitly DISCARDs uint64 truncation (`F-LOW-R10 NOT FIXED (intentional)`). 
- Adversarial: `uint64(idx * (liabBefore + amt) / liabBefore)` -Solidity 0.8 explicit narrow cast **TRUNCATES** silently (does NOT revert) for values exceeding `type(uint64).max`. With INIT_INDEX = 1e12 and `liabBefore = 1` (1-wei first-deposit), donating `~1.8e7` of token already overflows uint64. After truncation, `liquidityIndex` wraps to a small value ⇒ next depositor receives huge LP inflation. Documented as accepted constraint at lines 23-28 ("breach requires sustained donate-rebases against tiny liabBefore"). **F-A1-R14-2 (LOW, documented)**: 1-wei first-deposit + single donate is sufficient to wrap (not "sustained"). Documentation under-states the threshold. Mitigation: factory could front-run with seed-deposit, OR donate could revert on uint64 narrowing instead of silent truncation. Pre-existing known disposition; flagging for documentation accuracy only.

#### Pool.swapLiability -conservation
- `src/modules/Pool.sol:174-229`: no token push, no reserves change. liabilities adjust + lpBalances adjust. The R10 comment (lines 205-210) acknowledges `q.protoFee` intentional drop. Conservation holds because no flow occurs. ✓
- LP-share consistency: `lpAmountOut = liabOut * WAD / index_out` and `liabilities_out += liabOut` ⇒ existing LPs in `out` are diluted proportionally to liabIn's quoted-equivalent. This is by design (LP-bucket migration). ✓
- Edge: `assetOut.liabilities + liabOut > uint128.max` would revert (checked arith outside `unchecked`). ✓
- Edge: msg.sender LP-bal check (line 197) precedes liquidityIndex math; integer-overflow in `lpAmountIn * index_in` is in checked ctx → reverts on overflow. ✓

### A2 -auth / access

#### Previously un-audited -PoolProxy fallback + module-trust state machine
- `PoolProxy.fallback` (PoolProxy.sol:126-141): unimplemented selector ⇒ `Err.InvalidInput()` (line 128) BEFORE attempting delegatecall. ✓
- Selector clash: PoolProxy itself owns `initialize`, `setModuleTrustBatch`, `addModules`, `executeModuleUpdate`, `isModuleTrusted`, `moduleTimelocks`, `pendingModules`, `trustedModules`. These are concrete functions -call-resolution short-circuits via Solidity's built-in dispatch BEFORE fallback. ✓ HOWEVER: if a DEPLOYER registers a module selector that **matches one of PoolProxy's own concrete selectors** via `addModules`, Solidity dispatch wins and `$.modules[sel]` mapping is dead-storage → no security issue but silent no-op. **F-A2-R14-1 (INFO)**: `addModules` does not reject selectors that collide with PoolProxy's own functions; harmless but confusing. Consider adding a selector-blacklist or revert-on-collision.
- DEPLOYER role (PoolProxy.sol:18, 27): `immutable`, set once. Lines 38-44 explicitly DISCARD trust-revocation-on-rotation. The note that "addModules + executeModuleUpdate timelock flow re-validates trust at exec time" -confirmed at line 117 (`isModuleTrusted(newImpl)`). Trust model coherent. ✓
- `addModules` (PoolProxy.sol:90-109): For `existing == address(0)` registers instantly (no timelock). Combined with `setModuleTrustBatch`, DEPLOYER can extend the diamond with new selectors at any time without governance. Justified as "deployment role". ✓
- `addModules` race: if DEPLOYER calls twice for same selector with two different impls, second call **overwrites** first call's `pendingModules[sel]` AND resets `moduleTimelocks[sel]` to fresh delay. No `cancelModuleUpdate` exists. Only DEPLOYER can call ⇒ self-grief only. ✓

#### Bridge.recoverFailedMessage / refundFailedMessage
- `src/Bridge.sol:235-244`: deletes failedMessage BEFORE `crosschainMint` ⇒ replay-safe; if mint reverts, whole tx reverts and entry is restored. ✓ owner-only ✓ nonReentrant ✓.
- `recoverFailedMessage` BYPASSES `_tryCheckAndUpdateLimit` (no inbound bucket increment). If a flood of failedMessages were queued during a peer-removal window, owner-recovery floods inbound supply without bucket accounting. Owner-trusted, intentional (recovery is policy override).
- `refundFailedMessage` (Bridge.sol:247-269): delete-before-send. If `LZEndpointV2.send` reverts, whole tx reverts → safe. owner-only ✓.

#### Distributor -Merkle/cap/ledger
- `claimCampaign` (Distributor.sol:93-120): `msg.sender == account` check ✓; cumulative-claim ledger; cap enforcement at line 110 (totalClaimed ≤ totalAllocated); fund-availability check before transfer. Merkle leaf `keccak256(abi.encodePacked(index, account, totalEarned))` -**does NOT include campaignId**. **F-A2-R14-2 (LOW)**: a manager that publishes the same root across two campaigns enables cross-campaign claim-replay (proof valid for campaign A also valid for campaign B). Only manageable via off-chain manager hygiene. Cite for mitigation comment OR include `campaignId` in leaf domain-separation.
- **F-A2-R14-3 (MED, NEW): campaign 0 is permanently unreachable on all mutating paths.** `nextCampaignId` defaults to 0; first call `createPointsCampaign` / `createTokenCampaign` does `campaignId = ds.nextCampaignId++` returning 0; sets `c.id = 0` (Distributor.sol:42, 56). Then `_campaign()` (line 27) checks `if (c.id == 0) revert NotConfigured(...)` ⇒ EVERY mutating entrypoint (`claimCampaign`, `pauseCampaign`, `resumeCampaign`, `finalizeCampaign`, `finalizePointsCampaign`, `redeemPoints`, `updateCampaignRoot`) reverts for the first-created campaign. The campaign exists in storage but is permanently inert. SBT (for POINTS type) is deployed and orphaned. Severity MED: real DoS + stranded SBT contract; trivially worked around (admin creates a dummy first campaign), but no on-chain warning surfaces it. Recommend: initialize `nextCampaignId = 1` in storage init OR replace `c.id == 0` existence check with a sentinel-bit flag.

#### InternalOracle.pushFeedInternal -context requirement
- `src/modules/InternalOracle.sol:124-134`: `if (msg.sender != address(this)) revert Unauthorized()`. Self-call only -invoked from Pool's swap path via delegatecall to InternalOracle module address (via diamond). Cannot be griefed externally. ✓
- `updateFeed` (line 84-115): `msg.sender == _owner() || msg.sender == address(this)`. Self-call permitted because `_setupOracleAndConfig` (Admin.sol:439) calls via `InternalOracle(address(this)).updateFeed(...)` -direct external call. Owner-side path resets `priceAccB64=0`, `fastSnapB64=0`, `slowSnapB64=0` ⇒ wipes TWAP state. Owner-trusted; cited prior round as design.

#### ExternalOracle deviation bounds
- `src/oracles/ExternalOracle.sol:104`: explicit comment "maxDeviation event-only; deviation checks done off-chain pre-push". On-chain, any granted oracle key can push arbitrary `(fastEMA, slowEMA, fastVolEMA, slowVolEMA)` subject only to `_validate` (non-zero, vol < MAX_VOLATILITY). **F-A3-R14-1 (LOW, design)**: oracle-key compromise ⇒ unbounded price push. Not a sequencer concern (push-based not pull). Documented; mitigation lives in oracle key governance + off-chain monitoring. Not actionable on-chain without breaking design.

### A3 -economic / MEV

- Sandwich/donation/oracle: prior fixes hold. Anchor-path-quote re-derived (LibPricing -leaning on R7+R10 disposition). No new finding.
- `redeemPoints` (Distributor.sol:165-190): cap-check before state mutation, funding check before burn, SBT burn before redeemToken transfer ⇒ atomic. ✓
- `Bridge` rate-limit edges post-R10/R11/R13: re-verified equality boundary semantics symmetric across `_tryCheckAndUpdateLimit`, `_checkAndUpdateLimit`, `getRemainingLimits`. ✓

### A4 -numerical

- `Flash.flashLoan` `unchecked { asset.reserves += uint128(fee - protoFee); }` -analyzed §2.2 above. Safe under realistic params.
- `LibAnchorTree.walkToRoot` `mstore(path, length)` truncation: sound, sets ABI length without realloc. ✓
- `Distributor` `ds.totalClaimed[campaignId] += claimable` then `if > c.totalAllocated revert` -order is correct (write before read for revert; on revert state reverts). ✓
- `InternalOracle._updateFeeds` (lines 155-189): all in `unchecked` -manual analysis: `dt = currentTime - acc.lastUpdate` early-returns on `dt == 0`; stale cap @ 7d; multiplications by `dt ≤ MAX_STALENESS = 604800` are bounded. `lastPriceInAccDec * dt` could overflow if `lastPriceInAccDec ≈ 1e76` (b64 max) and `dt = 6e5` ⇒ ~6e81 > uint256.max (1.16e77). **F-A4-R14-1 (LOW, theoretical)**: `_updateFeeds` line 174 unchecked overflow when accumulator pushes b64-extreme prices over 7-day stale-window. Realistic prices ≤ 1e30 leave headroom > 1e40, no real exposure. Bound by pre-validated b64Decimals tag (≤ 18) keeping `lastPriceInAccDec ≤ 1e18 + b64-mantissa ≈ 1e21 max realistic). INFO/coverage.

---

## 4. Findings table

| ID | Sev | File:Line | Status | Description |
|---|---|---|---|---|
| F-A2-R14-3 | **MED** | src/modules/Distributor.sol:27,38,53 | NEW | First created campaign (id=0) permanently inert on all mutating paths. `_campaign()` rejects `c.id == 0`; first `nextCampaignId++` returns 0. SBT orphaned. Workaround trivial but unsignalled. |
| F-A1-R14-1 | LOW | src/modules/Admin.sol:94-104 | NEW | `setAnchor` does not cascade depth-revalidation to descendants; reshuffle can silently push descendants beyond MAX_DEPTH causing routing reverts until owner re-anchors. |
| F-A2-R14-2 | LOW | src/modules/Distributor.sol:226 | NEW | Merkle leaf lacks `campaignId` domain separation; cross-campaign proof reuse possible if manager reuses root. Off-chain manager hygiene only. |
| F-A3-R14-1 | LOW | src/oracles/ExternalOracle.sol:104,126 | DESIGN | On-chain `maxDeviation` is event-only; oracle key compromise ⇒ unbounded push. Documented design choice. |
| F-A1-R14-2 | LOW | src/modules/Pool.sol:23-28,86 | DOC | uint64 `liquidityIndex` truncation threshold under-stated (single-shot 1-wei + donate suffices, not "sustained"). DISCARD comment accuracy issue. |
| F-A2-R14-1 | INFO | src/PoolProxy.sol:90-109 | NEW | `addModules` allows registering selectors that collide with PoolProxy's own concrete functions; harmless silent no-op. |
| F-A4-R14-1 | INFO | src/modules/InternalOracle.sol:155-189 | THEORETICAL | `unchecked` block multiplies decoded price × dt; b64-extreme inputs at MAX_STALENESS could overflow uint256. Realistic price ranges leave > 40 orders of headroom. |

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH     | 0 |
| **MEDIUM** | **1** |
| LOW      | 4 |
| INFO     | 2 |

---

## 5. R14 verdict

**R14 = DIRTY** under strict exit reading (0 CRIT/HIGH/MED required for clean).

One MED finding (F-A2-R14-3, Distributor first-campaign DoS) is structural and pre-existing across all twelve prior rounds. Pattern continues to hold: each scope-widening pass uncovers latent defects in surface that previous rounds treated as out of scope or trivial.

Previously un-audited surfaces covered this round:
- PoolProxy fallback + addModules/setModuleTrust/executeModuleUpdate state machine.
- LibAnchorTree validation + walkToRoot + LCA + path construction (full re-walk).
- Pool.donate inflation/share-griefing (uint64 cast truncation analysis).
- Pool.swapLiability conservation re-derivation.
- InternalOracle.pushFeedInternal context invariants.
- InternalOracle._updateFeeds unchecked-block overflow analysis.
- ExternalOracle pushFeed deviation surface.
- Bridge.recoverFailedMessage + refundFailedMessage replay/auth.
- Distributor.claimCampaign + redeemPoints (Merkle leaf domain separation, cap, ledger).
- Distributor campaign-id-zero existence check.
- Admin.setAnchor descendant cascade.

R13 fixes themselves: all five FIXED correctly with no regressions.

---

## 6. Counter status

- R12 = DIRTY (2 HIGH).
- R13 = remediation commit (not a clean round, fixes the R12 findings).
- R14 = DIRTY (1 MED).

**Counter: 0 / 2.** Counter does not advance -F-A2-R14-3 must be remediated and a follow-up clean round must occur to begin counting.

---

## 7. Recommended R15 remediation

1. **F-A2-R14-3 (MED, REQUIRED)**: in `Distributor` storage init (or first-call guard), set `nextCampaignId = 1`. Alternative: drop `c.id == 0` existence check in `_campaign` and use a `bool exists` slot per campaign.
2. **F-A2-R14-2 (LOW)**: add `campaignId` to leaf hash: `keccak256(abi.encodePacked(campaignId, index, account, totalEarned))`. Coordinate with off-chain root signer.
3. **F-A1-R14-1 (LOW)**: `Admin.setAnchor` should walk `_descendants` and revert if any post-reshuffle depth > MAX_DEPTH. Or document acceptance via DISCARD comment.
4. **F-A2-R14-1 (INFO)**: `addModules` could `require(sel != PoolProxy.initialize.selector && ...)` to make collisions explicit. Cosmetic.
5. **F-A1-R14-2 (DOC)**: tighten the F-LOW-R10 DISCARD comment in Pool.sol to acknowledge "1-wei first-deposit + single-donate" suffices to wrap.
6. **F-A4-R14-1 / F-A3-R14-1**: theoretical / documented design -DISCARD comments acceptable.

After R15 remediation, restart 0/2 counter; need 2 sequential clean rounds for sealing.

End of R14.
