# 16. Phase 42C Round 1 -DEX Security Audit (4-Auditor Cohort)

Read-only audit, 4 parallel passes (A1=invariants/state, A2=auth/access,
A3=economic/MEV, A4=numerical/precision). Mirrors Phase 16 ALM exit criterion:
2 successive 0-CRIT/0-HIGH/0-MED rounds. This is Round 1.

---

## 1. Scope

**Target HEAD:** `dex/evm/src/` post Phase 42B (179/179 ✓ tests).
- `Bridge.sol` (449), `PoolProxy.sol` (129), `PoolProxyFactory.sol` (193),
  `Router.sol` (333), `Treasury.sol` (343).
- `modules/`: Admin (441), Base (304), Distributor (222), Flash (80),
  InternalOracle (226), Pool (533), Staking (207), Lend (9 stub).
- `libraries/`: LibAnchorTree (123), LibConstants (90), LibMaths (169),
  LibOracle (59), LibPricing (584), LibSpline (122), LibTimelock (31),
  LibTransientCache (61), LibUpgradeQueue (57).
- `oracles/ExternalOracle.sol` (211).
- Peripheral consumed: `Errors.sol`, `access/AccessControl.sol` (168) -singleton
  AC, in-scope only via the IAccessControl shim consumed by `Base._owner()`.

ALM and ABIs (sdk) are out of scope. Tests, scripts, and `out/` not audited.

---

## 2. Auditor pass summaries

### A1 -Invariants & State

Reviewed: storage layouts (ERC-7201 slots in `LibConstants`), reserves/liabilities
accounting (`Pool.deposit/withdraw/swap`, `_exec`, `_reconcile`), oracle accumulator
state (`InternalOracle._updateFeeds`), Bridge daily limit accounting
(`_checkAndUpdateLimit`), Treasury vesting + emissions (`claimVested`,
`_checkAndUpdateEmissions`), staking lock counters.

Findings: see §3 (#A1-1, #A1-2).

Notable invariants confirmed:
- `Pool._exec` (Pool.sol:432-444): pre-decrement reserve check `aOut.reserves >= q.amountOut + minLiquidity` ✓
- `Flash.flashLoan` (Flash.sol:52-55): `balanceAfter >= balanceBefore + amount + fee` post-call ✓
- `Distributor.claimCampaign` (Distributor.sol:101): cumulative-merkle pattern
  `campaignClaimed[id][a] = totalEarned` (NOT `+= claimable`) → replay-safe ✓
- Treasury maxSupply enforcement on every mint path ✓
- `Pool.donate` (Pool.sol:80-81): `liquidityIndex *= (liabBefore+amt)/liabBefore`
  preserves per-LP claim ratio ✓

### A2 -Auth & Access

Reviewed: `Base._owner()` Path α fall-through, `Base.onlyOwner` modifier, every
`onlyOwner` site across Admin/Pool/Bridge/Treasury/Router/Distributor/Staking,
peripheral `AccessControl` (2-step + bootstrap + timelocks), PoolProxy module
trust gate, Router executeSwap pool authentication, Bridge.lzReceive peer
trust + msg.sender check, `setAc` gating, `executeOwnershipTransfer` semantics.

Findings: see §3 (#A2-1 HIGH, #A2-2 HIGH, #A2-3 MED, #A2-4 LOW, #A2-5 INFO).

### A3 -Economic & MEV

Reviewed: Router multi-hop quote (1e18 probe + 5% MIN_HOP_IMPROVEMENT), executeSwap
intermediate `minAmountOut=0` per-hop, batchSwap weight rounding, Distributor
`updateCampaignRoot` funding precondition, Pool spline dispersion, Treasury
vesting cliff/post-cliff math, Flash fee model, oracle TWAP fallback to spot.

Findings: see §3 (#A3-1 HIGH, #A3-2 MED, #A3-3 MED, #A3-4 LOW, #A3-5 LOW).

### A4 -Numerical & Precision

Reviewed: `LibPricing._powWad` hand-rolled vs Solady candidates, B64 packed
encoding/decoding (`LibMaths`), oracle accumulator overflow guards, fee splits,
inventory skew clamping `[-100,+100]`, dispersion clamp, `_skewToDepth`
unchecked sign, decimals scaling in `_executeLeg`, `_calculateDecay` cap.

Findings: see §3 (#A4-1 MED, #A4-2 LOW, #A4-3 LOW, #A4-4 INFO).

---

## 3. Findings table

| # | Sev | File:Line | Title | Repro | Fix sketch |
|---|---|---|---|---|---|
| A2-1 | **HIGH** | `PoolProxy.sol:98-107` | `executeModuleUpdate` lacks access control | Anyone can call once timelock elapses. While the impl was already authorized via `addModules` (DEPLOYER) + a trusted-codehash check, anyone can race the executor and front-run the legitimate execution flow (potentially mid-init). Worse: `addModules` itself permits a non-deployer to be unable to call but the diamond-fallback `executeModuleUpdate(bytes4)` selector is *also* exposed via `fallback()` if shadowed. Permissionless execute is intentional for some patterns but here it bypasses the documented "owner-driven" upgrade flow. | Add `if (msg.sender != DEPLOYER) revert Ownable.Unauthorized();` to `executeModuleUpdate`, OR document explicitly that exec is permissionless and re-validate trust + timelock atomically (already done). Reclassify to LOW if intentional + documented. |
| A2-2 | **HIGH** | `Treasury.sol:206-214` | `executeOwnershipTransfer` permissionless | No `onlyOwner` modifier. `TL.validate(pendingOwnershipOp)` is the only gate. Once timelock elapses, any external party can finalize the transfer (and *also* any party can finalize even after grace expires?? -`TL.validate` with `grace>0` reverts past `eta+grace`, OK). The gating relies entirely on the *pre-queued* state. This is acceptable given timelock + cancel paths, but the `requestOwnershipTransfer` signer (`onlyOwner`) is not guaranteed to be the eventual executor → cancel-then-replay race. Same pattern in `executeEmissionsCapChange:251` and `Router.executeUpgrade:315`. | Either gate `executeOwnershipTransfer` with `onlyOwner` (and also `executeEmissionsCapChange`, `Router.executeUpgrade`) for symmetry with cancel paths, OR document the permissionless-execute pattern + emit the executor address in the event. |
| A2-3 | MED | `Router.sol:241-271` | `executeSwap` calls non-validated pools | `executeSwap` accepts `Route calldata` from arbitrary user; per-step `IExchange(step.pool).swap{value:...}` is invoked WITHOUT verifying `step.pool` is a registered pool via `factory.isPool(step.pool)`. Note `executeBatchSwap:280` *does* verify. An attacker could craft a Route pointing at an attacker contract that drains `currentToken` from Router (Router holds tokens transiently after `safeApprove`). Mitigation: Router holds nothing across calls (`nonReentrant` + push-out at end). However `safeApprove(step.pool, currentAmount)` on a malicious pool grants allowance that the attacker then transfers via transferFrom. | Add `if (!IPoolProxyFactory(factory).isPool(step.pool)) revert Ownable.Unauthorized();` per step. Reset approvals to 0 post-call. |
| A2-4 | LOW | `Treasury.sol:236-240` | `authorizeRemoteDistributor` overwrites silently | No "old==new" check; setting same value still emits event. Setting to nonzero after a prior nonzero overwrites without timelock; given Treasury is owner-multisig-trusted this is acceptable but inconsistent with treasury/distributor setters and fits a timelock policy on cross-chain trust assumptions. | Add timelock or at minimum add a same-value short-circuit and emit old→new. |
| A2-5 | INFO | `Base.sol:42-47` | Path α AC fall-through fails open on AC mis-set | If `$.ac` is set to a contract without a working `owner()` method, the static call reverts and the entire pool becomes inaccessible (every `onlyOwner` reverts). Bricking risk on misconfiguration; intentional per docstring ("mis-set ac is operator error"). | Document recovery path: any owner-gated `setAc(0)` would itself be locked out. Suggest a constructor-immutable rescue: `if ac.code.length == 0 → fall through to legacy owner` to prevent EOA/zero-code lockout. |
| A2-6 | LOW | `Admin.sol:172-197 / 220 / 243-249 / 256 / 268 / 284 / 297` | `executeXxx` paths gated `onlyOwner` while Treasury equivalent is permissionless | Asymmetry: Admin's `executeOwnershipTransfer:231` is NOT `onlyOwner`, while the rest of the executes are. This is consistent with timelock-only gating BUT inconsistent with sibling executes in Admin itself. | Either: gate `executeOwnershipTransfer` consistently OR ungate the rest for symmetry. |
| A1-1 | MED | `Pool.sol:436` | `_exec` reserve credit uses half-spread asymmetrically | `aIn.reserves += amtIn - (amtIn * spreadBps / 2) / 1e6`. The half-spread is deducted on the IN side only; the OUT-side half is taken via `q.amountOut` already discounted in LibPricing. Net economic effect = full spread, BUT the IN-side deduction is *not* tracked anywhere -it's silently subtracted from the user's reserve credit. Effectively a "phantom fee" that improves coverage on tokenIn without ever crediting `protocolFees` or `liabilities`. | If intentional (donation to LP coverage), document it. If not, route `(amtIn * spreadBps/2)/1e6` to `$.protocolFees[tkIn]` or to `aIn.liabilities` reduction explicitly. |
| A1-2 | LOW | `InternalOracle.sol:60-66` | `getFastTWAP` returns spot, not TWAP | Documented as "returns spot (not real TWAP)" in `@dev`. Misleading name; downstream callers reading "fastTWAP" via this fn get spot price → flash-loan oracle attack vector for any consumer of this view. Currently no in-tree consumer uses it (verified by grep). | Rename `getFastTWAP` → `getSpotPrice`, or compute the actual fast TWAP via `acc.fastOffset` decoding, or revert (not used). |
| A1-3 | LOW | `Bridge.sol:39 / 332-355` | Stale `pendingOps[upgradeId]` cleanup | `pendingUpgrade` keyed by `keccak(impl, block.timestamp)` -`cancelUpgrade` deletes the keyed entry, but if a SECOND `requestUpgrade` is invoked at a different block, a stale `pendingOps[oldId]` could remain (mitigated by single-pending guard at line 334). Not exploitable but is dead-state slot. | Confirm guard prevents this; add invariant test. |
| A3-1 | HIGH | `Router.sol:241-271` | `executeSwap` per-hop `minAmountOut=0` | `IExchange(step.pool).swap(... 0, address(this))` -slippage check is END-OF-ROUTE only (`amountOut < minAmountOut`). Sandwich attack on intermediate hop hands attacker delta but final check still passes if global minOut is loose. Combined with #A2-3, allows hostile pool to fully sandwich. | Pass per-hop minOut from Route struct OR enforce a global per-hop floor derived from quote at call time. |
| A3-2 | MED | `LibPricing.sol:505` | `netCoverageImpact` hard-coded "0.1% heuristic fee" | `uint256 totalOut = amountOut + (amountOut * 100) / 100000;` magic-number 0.1% baked in. Decouples from actual fee model (`feeParams.flashFeeBps` etc.). When real fee >> 0.1%, impact estimate underestimates reserve consumption → spread under-prices large trades; when real fee << 0.1%, overestimates. | Replace 100/100000 with the actual `quote.spreadBps / 1_000_000` or pass fee from caller. |
| A3-3 | MED | `Router.sol:212` | Best-pool 1e18 probe ignores price impact | `_findBestPoolForPair` uses fixed `1e18` probe → picks pool with best mid-price, NOT best execution for the actual `amountIn`. Large swaps route through shallow pools with high impact. | Probe with actual `amountIn` (gas hit) OR add a depth heuristic via `getPoolTokens` reserves view. |
| A3-4 | LOW | `Distributor.sol:88-112` | Cumulative-merkle replay-safe but `c.totalAllocated` not enforced | `claimCampaign` decrements via cumulative (`totalEarned ≤ claimed` returns 0), good. However nothing enforces `ds.totalClaimed[campaignId] ≤ c.totalAllocated`; if an off-chain root signer mistakenly publishes a tree summing > `totalAllocated`, distributor over-spends silently up to balance. | Add `if (ds.totalClaimed[campaignId] + claimable > c.totalAllocated) revert ...` post-claim. |
| A3-5 | LOW | `InternalOracle.sol:143` | Same-block update silently dropped | `if (dt == 0) return;` -multiple swaps in the same block don't update TWAP. Manipulation guard ✓ but means the *first* swap's price persists for the block; last-swap-wins not applied. Front-runner can pin oracle to first swap. | Acceptable per design; document explicitly as "first-swap wins for TWAP within a block". |
| A4-1 | MED | `LibPricing.sol:565-583` | `_powWad` hand-rolled approx imprecise | Comment: "SECURITY: hand-rolled, defer Solady swap to audit." Linear approximation `(x_remainder term)/10` for fractional exponent -for `x` near 0 (high coverage deficit + low amplifier), error grows; result feeds `calculateDepth` → spread/depth pricing. Solady `FixedPointMathLib.powWad` exists (signed wad pow) -switch or unit-test boundary cases (`x=1`, `x=WAD`, `y=WAD`, `y=2*WAD`, `x→0`). | Replace with Solady `FixedPointMathLib.powWad(int256(x), int256(y))` (signed→unsigned cast) or equivalent. |
| A4-2 | LOW | `LibPricing.sol:386-389` | Decimal scaling can underflow `priceOut` to 1 | `if (priceOut == 0) priceOut = 1;` -silent zero-clamp. For very small `amountOut/amountIn` ratios with mismatched decimals, exec price floors at 1 (smallest unit), which then encodes via `M.encodeB64` and propagates as oracle `lastPriceB64`. This poisons the InternalOracle accumulator. | Revert on `priceOut==0` or skip oracle push (`q.hopPrices[i] = 0` and check in `_oracle`). |
| A4-3 | LOW | `Pool.sol:436` | Spread half rounds down | `(amtIn * q.spreadBps / 2) / 1_000_000` -integer division of `spreadBps/2` truncates odd basis points to floor. Negligible (1 PBPS) but consistent rounding-to-LP would prefer ceil. | Use `(amtIn * q.spreadBps + 1_000_000) / (2 * 1_000_000)`. |
| A4-4 | INFO | `LibPricing.sol:218-220` | `_skewToDepth` unchecked add | `unchecked { return uint256(5000 + int256(skew) * 50); }` -`skew ∈ [-100,+100]` enforced by `computeInventorySkew` clamps → range `[0, 10000]`. Safe given input invariant. | Document the precondition; add invariant assertion in debug builds. |
| A4-5 | INFO | `ExternalOracle.sol:18 / 95-101` | `MAX_DEV_THRESHOLD` event-only | "NB: maxDeviation event-only; deviation checks done off-chain pre-push" -admin can emit a deviation cap that on-chain code never enforces. Acceptable per docstring; UX risk: integrators may mistakenly trust the cap. | Rename event field to `maxDeviationOffchainHint` or remove. |

**Counts:** CRIT 0 · **HIGH 3** (A2-1, A2-2, A3-1) · **MED 5** (A2-3, A1-1, A3-2, A3-3, A4-1) · **LOW 7** (A2-4, A2-6, A1-2, A1-3, A3-4, A3-5, A4-2, A4-3) · **INFO 3** (A2-5, A4-4, A4-5).

Cross-validation:
- A2-1 (PoolProxy permissionless exec): A2 + A1 concur (state-mutation risk).
- A2-2 (Treasury exec OT): A2 + A1 concur.
- A3-1 (per-hop minOut=0): A2 + A3 concur (auth+economic), tied to A2-3.
- A1-1 (phantom half-spread): A1 + A4 concur (accounting + numerical).

---

## 4. Deferred-item resolution (11 items from Phase 42B)

| # | Item | Resolution | Note |
|---|---|---|---|
| 1 | Router best-pool 1e18 probe price-impact | **OPEN → #A3-3 MED** | Probe ignores `amountIn` shape. |
| 2 | Treasury custom 2-step ownership | **OPEN → #A2-2 HIGH** | Permissionless `executeOwnershipTransfer`. |
| 3 | Bridge.lzReceive queue replay/order | **RESOLVED** | `_guid`-keyed `failedMessages`, peer trust verified pre-queue, `crosschainMint` re-throws caught → no replay. `recoverFailedMessage` deletes pre-mint. ✓ |
| 4 | Admin.executeOwnershipTransfer onlyOwner | **OPEN → #A2-6 LOW** | Asymmetric vs sibling executes. |
| 5 | Distributor.claimCampaign auth | **RESOLVED** + side-finding A3-4 | `msg.sender == account` check + cumulative-merkle prevent replay. `totalAllocated` cap unenforced (LOW). |
| 6 | Admin._updateModule arbitrary delegatecall | **RESOLVED w/ caveat** | Owner-gated + timelock. Selector-collision risk mitigated by `bytes4[] selectors` explicit list. Init-reentry: `delegatecall(initData)` runs in proxy storage, but `nonReentrant` not held across, so init is not reentrant-protected. → mark **INFO**: document that init code MUST be self-contained. |
| 7 | InternalOracle.getFastTWAP returns spot | **OPEN → #A1-2 LOW** | Misleading name; no in-tree consumer. |
| 8 | Liquidity._applyHaircut vs LibPricing consistency | **RESOLVED** | `_applyHaircut` (Pool.sol:218-232) deficit×(1-suppressor/20000) capped 1e18. LibPricing routes via spline + Avellaneda skew; haircut is a withdraw-only safety net, not pool invariant pricing. Consistent. ✓ |
| 9 | ExternalOracle.maxDeviation default safety | **RESOLVED w/ INFO** | `MAX_DEV_THRESHOLD = 65_000` (6.5% in BPS×10). Event-only enforcement (#A4-5 INFO). Constructor doesn't default-set; admin must call `addFeed` with explicit value. ✓ |
| 10 | PoolProxy.codehash trust attestation | **RESOLVED w/ caveat** | `setModuleTrustBatch` (DEPLOYER-only) maps `codehash → trusted`. `executeModuleUpdate` re-validates at exec ✓. Caveat: codehash trust persists across DEPLOYER turnover; if DEPLOYER is rotated, prior-trusted hashes remain. → **INFO**: add `revokeModuleTrust` or rely on owner-by-codehash redeploy. |
| 11 | LibPricing._powWad numerical correctness | **OPEN → #A4-1 MED** | Hand-rolled, comment requests Solady swap. |

**Resolved:** 5 (#3, #5, #6, #8, #9, #10 with caveats)
**Open as findings:** 6 (#1, #2, #4, #7, #11 + new A3-3/A2-2/A2-6/A1-2/A4-1)

---

## 5. Round 1 verdict -DIRTY

3 HIGH + 5 MED. Does NOT meet 0-CRIT/0-HIGH/0-MED threshold. Round 2 cannot
proceed until findings A2-1, A2-2, A3-1 are resolved (or formally re-classified
with debate notes), and the MED bucket is addressed.

Exit criterion (2 successive 0-finding rounds) not met. Earliest possible clean
round = Round 2 *after* fix batch.

---

## 6. Action list for Round 2

### Must-fix (HIGH):
1. **A2-1 PoolProxy.executeModuleUpdate** -add `if (msg.sender != DEPLOYER) revert Ownable.Unauthorized();` OR explicitly document permissionless intent + emit executor address. (`PoolProxy.sol:98`)
2. **A2-2 Treasury.executeOwnershipTransfer** -add `onlyOwner` for symmetry with `cancelOwnershipTransfer`. Likewise `executeEmissionsCapChange:251` and `Router.executeUpgrade:315`. (`Treasury.sol:206`, `Router.sol:315`)
3. **A3-1 Router.executeSwap per-hop minOut=0** -extend `RouteStep` with `minOut`, propagate from quote, gate against sandwich. (`Router.sol:241-271`)

### Should-fix (MED):
4. **A2-3 Router.executeSwap pool validation** -`factory.isPool(step.pool)` per step + reset approvals to 0 post-call. (`Router.sol:258-263`)
5. **A1-1 Pool._exec phantom half-spread** -route the IN-side half-spread to `$.protocolFees[tkIn]` or document as donation. (`Pool.sol:436`)
6. **A3-2 LibPricing.netCoverageImpact** -replace 0.1% magic number with actual fee; or pass fee from caller. (`LibPricing.sol:505`)
7. **A3-3 Router._findBestPoolForPair** -probe with actual `amountIn`, not 1e18. (`Router.sol:212`)
8. **A4-1 LibPricing._powWad** -replace with Solady `FixedPointMathLib.powWad`. (`LibPricing.sol:565`)

### Nice-to-have (LOW/INFO):
- A2-4 timelock on `authorizeRemoteDistributor`.
- A1-2 rename `getFastTWAP` → `getSpotPrice` or revert.
- A1-3 invariant test on Bridge upgrade pendingOps cleanup.
- A3-4 enforce `totalClaimed ≤ totalAllocated` in Distributor.
- A4-2 don't clamp `priceOut` to 1; revert or skip oracle push.
- A4-3 ceil-rounding on half-spread.

### Round 2 entry condition:
- All 3 HIGH addressed (fix or debate-note dissent w/ ≥ 2 auditor sign-off).
- All 5 MED addressed.
- LOW/INFO triaged (defer or fix; not blocking).
- Re-run 4-auditor cohort + 179/179 ✓ tests.
