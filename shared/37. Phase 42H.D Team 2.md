# Phase 42H.D · Team 2 -Independent 4-Expert Re-Score (Post G1–G5 Remediation)

**Mode:** adversarial. Re-derived; no rubber-stamp of Team 1 conclusions.
**Reviewers:** A. Architect · B. Security · C. DeFi-Composability · D. Optimizer.

## 1. State snapshot

| Repo | LOC (src) | Tests | Build |
|---|---:|---:|---|
| `dex/evm/src` | 6,233 (Pool 1219, Staking 299, Distributor 188, Flash 85, PoolOracle 146 NEW) | 200/200 ✓ | clean (2 pre-existing forge-lint warns only) |
| `alm/evm/src` | 1,596 | 263/263 ✓ | clean |
| `shared/src` | ~460 (Timelock +13 NatSpec) | (consumed) | clean |
| `sdk` | 0.4.0 | typecheck ✓ | Pool ABI exposes all 5 oracle constants ✓ |

Recent: 8e3f140 dex (G1+G2+G3+G5) · d8326fd shared (G3 NatSpec) · 187fb20 docs (G4 ADR-001).

## 2. G1–G5 verification

| ID | Verdict | Evidence |
|---|---|---|
| G1 (Pool oracle extraction) | **PARTIAL** | `dex/evm/src/libraries/PoolOracle.sol:32-145` exists, 146 LOC; `Pool.sol:22` imports; `Pool.sol:450-456` `_pushFeedInternal` delegates. **BUT** Pool.sol = 1219 LOC (Team 1: 1320 → −101). Target was <1000; **misses target by 219 LOC**. ABI-stable mirror constants (`Pool.sol:89-95`) re-bloat surface. |
| G2 (ReentrancyGuardTransient) | **PARTIAL** | Pool.sol:14, Staking.sol:9, Distributor.sol:8, Flash.sol:9 → all on solady transient ✓. **Regression vector:** Treasury.sol:12, Bridge.sol:10, Router.sol:9 still on **non-transient** `ReentrancyGuard`. Same protocol now mixes 2 reentrancy primitives in 7 contracts. Team 1 listed only 4 dex contracts but did NOT widen to UUPS trio -Team 2 flags this as G2 residual. |
| G3 (Timelock NatSpec) | **PASS-as-doc** | `shared/src/Timelock.sol:13-24` documents 3 divergent encodings (AC `Queued`, Swapper `Pending`, PoolFactory raw `uint256`). Honest waiver, not unification. Code shape unchanged -gap remains real, just labeled. |
| G4 (UUPS ADR) | **PASS** | `docs/shared/36. ADR-001 Upgrade Strategy.md:1-121` records per-contract pattern. Acknowledges Treasury single-key vector + 7d window + kill switch. ADR explicitly defers Treasury hardening. |
| G5 (gas + storage) | **PARTIAL** | G5.b ✓ -`Pricing.sol:166` builds `points` once, reused L179 + L184 (saves ~5–8k gas/swap). G5.a ✗ -`Pool.sol:25-42` NatSpec documents the slot-0 / append-only constraint but **no structural-hash test** guarding upgrade safety; relies on developer discipline. G5 immutable getters (4× `address public immutable` Pool:61-70) **untouched**. |

**Net:** 2/5 PASS (G3-as-doc, G4) · 3/5 PARTIAL (G1, G2, G5). Treasury/Bridge/Router reentrancy mismatch is a **new gap surfaced by Team 2** that Team 1 missed.

## 3. Per-criterion scoring (1–10, 4 experts, INDEPENDENT)

| # | Criterion | A | B | C | D | Avg | ≥9.5 |
|---|---|---:|---:|---:|---:|---:|---|
| 1 | Novelty (spline AMM, AnchorTree N-token, hybrid 4626/7540) | 9.6 | 9.5 | 9.5 | 9.5 | **9.53** | ✓ |
| 2 | Governance (singleton AC, atomic, kill, timelocks, ADR-001) | 9.5 | 9.3 | 9.5 | 9.4 | **9.43** | ✗ |
| 3 | DeFi composability (4626/7540/3156/hooks/clones) | 9.6 | 9.4 | 9.7 | 9.5 | **9.55** | ✓ |
| 4 | Hyper-optimization (transient, packing, gas) | 9.4 | 9.3 | 9.3 | 9.4 | **9.35** | ✗ |
| 5 | Conciseness (LOC, dead-code, dup) | 9.3 | 9.2 | 9.2 | 9.2 | **9.23** | ✗ |
| 6 | Cross-repo alignment (alm↔dex↔shared↔sdk) | 9.2 | 9.3 | 9.3 | 9.2 | **9.25** | ✗ |
| 7 | Test rigor (regression+unit+invariant; **zero direct PoolOracle tests**) | 9.2 | 9.0 | 9.2 | 9.1 | **9.13** | ✗ |
| 8 | Naming + readability (NatSpec, mirror constants, lib `as M`) | 9.5 | 9.5 | 9.5 | 9.5 | **9.50** | ✓ (border) |

**Mean: 9.37.** Three of eight criteria reach ≥9.5; five remain in the 9.13–9.43 band.

## 4. Top strengths

1. **Spline + AnchorTree novelty intact** -`Pricing.sol:166` G5.b cache materialises a measurable win without touching the analytical core (`Spline.sol:42` Hermite-cubic `area()` integral).
2. **ADR-001 written as a real engineering doc** -`docs/shared/36. ADR-001 Upgrade Strategy.md:88-96` enumerates considered alternatives + explicit rejections. Acknowledges Treasury single-key vector instead of hand-waving.
3. **Solady ReentrancyGuardTransient on Pool/Staking/Distributor/Flash** (`Pool.sol:14,44`, `Staking.sol:9,23`, `Distributor.sol:8,19`, `Flash.sol:9,21`) -the four hot-path contracts now share the alm primitive. ~130 LOC of yul deletion as advertised.

## 5. Top gaps preventing 9.5+

### NEW -G2-residual: reentrancy primitive bifurcation
`Treasury.sol:12,21` + `Bridge.sol:10,20` + `Router.sol:9,18` still inherit `solady/utils/ReentrancyGuard.sol` (storage-slot guard). Pool/Staking/Distributor/Flash use `ReentrancyGuardTransient` (TSTORE). Same protocol → **two reentrancy primitives** is exactly the cross-repo idiomatic mismatch Team 1 was trying to close. ~14 `nonReentrant` call-sites affected.
**Fix:** swap to `ReentrancyGuardTransient` in the UUPS trio (no storage-layout impact since UUPS contracts use proxy-namespaced layout).

### G1-residual -Pool.sol still 1219 LOC
Target was <1000. Liquidity ops (564–738), exchange (759–927), admin gateways (~1009–1107) remain monolithic. Mirror constants (`Pool.sol:89-95`) re-export 5× lib values into the public ABI for stability -defensible but adds surface.
**Fix:** further extract `_executeSwap` family OR remove ABI mirror constants and route consumers (sdk) through `IPool` only.

### G5.a -no upgrade-safety guard
`Pool.sol:25-42` documents append-only invariant; **no test**. A future PR reordering `IPool.PoolStorage` (interfaces/IPool.sol:120) silently corrupts every live clone. ADR explicitly relies on developer discipline.
**Fix:** snapshot test asserting `keccak256(abi.encode(layout))` equals a pinned hash, or storage-slot diff test using `forge inspect Pool storageLayout`.

### G7 -zero direct PoolOracle tests
`grep -rn PoolOracle dex/evm/test/` → 0 hits. Coverage is purely indirect via Pool integration tests. New library surface lacks unit tests for `rollWindow` boundary conditions (snapAcc > currentAcc branch, MAX_STALENESS clamp, B64 decimal decode).
**Fix:** `test/unit/PoolOracleTest.t.sol` exercising the 3 internal helpers with synthetic accumulator state.

### G6 -Timelock divergence documented but not closed
`shared/Timelock.sol:13-24` lists 3 parallel encodings as "intentional, documented". Cross-repo alignment reviewer (C) cannot score this 9.5 when 4 timelock shapes coexist (`Timelock.uint96`, AC `Queued`, Swapper `Pending`, PoolFactory raw `uint256`). Doc-only fix punts the structural debt.

## 6. Pass/Fail vs golden-rule gate

**FAIL** -9.37 mean; 5/8 criteria below 9.5. G1 missed LOC target (1219 vs <1000); G2 only half-applied (UUPS trio still on storage-guard); G5 has no upgrade-safety test; G7 has zero direct tests on the new library surface; G6 documented but unresolved.

The remediation is **directionally correct but execution-incomplete**. Tests pass and builds are clean -that bar was met in Team 1 too. The 9.5 gate requires structural fixes, not green-CI signal.

## 7. Counter status

**0/2 sequential 9.5+.** Team 1 = FAIL (9.31). Team 2 = FAIL (9.37). Counter remains **0/2**, no restart triggered.

## 8. Sequential-pair determination

Team 2 did **not** pass ⇒ sequential-pair counter does **not** advance. Two consecutive clean teams still required. Recommended next steps before Team 3:
1. Migrate Treasury/Bridge/Router to `ReentrancyGuardTransient` (closes G2 fully).
2. Add `test/unit/PoolOracleTest.t.sol` (closes G7 partially).
3. Add `forge inspect Pool storageLayout` snapshot test (closes G5.a).
4. Either further-decompose Pool.sol toward <1000 LOC OR formally accept 1219 in ADR-002.
5. Either lift `Queued`/`Pending` into `shared/` OR move PoolFactory's raw `uint256` to `Timelock.pack` (closes G6 structurally).
