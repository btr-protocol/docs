# 14. Cohort A Round 4 -3-Expert Re-Evaluation (gate 2/2)

Read-only re-pass following the 2-LOC Path α residual remediation (`9e3670f`). Round 3
identified 2 raw `_s().owner` reads that bypassed the singleton AC fall-through; this
round verifies the patch and sweeps for any remaining leak. Goal: 6/6 ≥ 9.5 to satisfy
the **golden rule** -two sequential 9.5+ rounds.

---

## 1. State snapshot

| Surface | Path | Files / LOC | Build | Tests |
|---|---|---|---|---|
| alm | `alm/evm/src/` | 5 / 1,727 | ✓ | **263 / 263 ✓** |
| dex | `dex/evm/src/` | 24 / ~6,430 | ✓ | **173 / 173 ✓** |
| peripheral src | `peripheral/src/` | `Errors.sol` 72 + `access/AccessControl.sol` 168 + `oracle/PriceProvider.sol` 136 = **376** | ✓ | -|
| peripheral test | `peripheral/test/` | `Common.sol` + `mocks/{4}` (~208) | ✓ | -|
| peripheral script | `peripheral/script/Deploy.base.sol` | 1 / 48 | ✓ | -|

Recent dex commits:
- `c1c84b9` -Path α (singleton AC bridge): `IPool.PoolStorage.ac` append +
  `Base._owner()` fall-through + `Admin.setAc/getAc` + `Distributor` re-route + 4
  PathAlphaACTest tests.
- `9e3670f` -Path α residuals: `InternalOracle.sol:77` and `Pool.sol:402` re-routed
  through `_owner()`.

Peripheral commit unchanged: `f3ce57d` (PP extraction).
Stage 3f no-op verdict (Round 3) preserved -no DRY rework executed.

---

## 2. Verification of `9e3670f` patch

### 2.1 InternalOracle.updateFeed gate

`dex/evm/src/modules/InternalOracle.sol:77`:

```solidity
if (msg.sender != _owner() && msg.sender != address(this)) revert Ownable.Unauthorized();
```

✓ Reads through `Base._owner()` (via inheritance -`InternalOracle` extends `Base`).
Singleton AC owner is honored when `_s().ac != 0`. The `address(this)` branch
preserves the self-delegate path used by initialization / governance plumbing -this
is **not** a parallel auth surface, it is an intentional intra-contract callback
allowance and is gated by upstream `onlyOwner`-bearing entry points.

### 2.2 Pool.owner() view

`dex/evm/src/modules/Pool.sol:402`:

```solidity
function owner() external view returns (address) { return _owner(); }
```

✓ External view now reflects active owner under both legacy and singleton paths.
External integrators (front, keepers, indexers) querying `pool.owner()` see a
consistent answer with the gate logic. This was the externally-visible Round 3 leak.

Both lines compiled and shipped under `9e3670f`; full 173/173 dex test suite passes
post-patch.

---

## 3. Sweep -additional `_s().owner` raw reads

Regex: `_s\(\)\.owner` across `dex/evm/src/`.

| File:Line | Context | Verdict |
|---|---|---|
| `src/modules/Base.sol:46` | `return ac == address(0) ? _s().owner : IAccessControl(ac).owner();` | **Legitimate** -this *is* the fall-through implementation inside `_owner()` itself. Required by design. |

**Hit count: 1. Leak count: 0.**

### 3.1 Sweep -owner-related call sites in modules

Regex: `owner\(\)|_owner\(\)` across `dex/evm/src/modules/`.

| File:Line | Use | Verdict |
|---|---|---|
| `Base.sol:38` | `onlyOwner` modifier -`msg.sender != _owner()` | ✓ |
| `Base.sol:44-46` | `_owner()` definition | ✓ |
| `Admin.sol:21` | local `onlyOwner` override -`msg.sender != _owner()` | ✓ |
| `InternalOracle.sol:77` | `msg.sender != _owner() && msg.sender != address(this)` | ✓ (post-`9e3670f`) |
| `Distributor.sol:144` | `msg.sender != _owner()` | ✓ |
| `Pool.sol:402` | `function owner() external view { return _owner(); }` | ✓ (post-`9e3670f`) |

All 6 governance call sites in modules now route through `Base._owner()`.

### 3.2 Sweep -parallel auth surfaces (`Ownable` imports outside modules)

| File | Surface | Verdict |
|---|---|---|
| `Treasury.sol`, `Bridge.sol`, `Router.sol`, `PoolProxyFactory.sol`, `PoolProxy.sol`, `oracles/ExternalOracle.sol`, `tokens/{StakedToken,SoulboundToken,GovToken}.sol` | Inherit Solady `Ownable` directly -these are **non-pool** contracts (treasury, bridge, router, factory, tokens, external oracle). They live outside the pool diamond storage. They use stock `Ownable` and are out-of-scope for Path α (which targets per-pool owner unification). | ✓ Not a leak -these are sibling contracts with their own owner lifecycle, by design. |
| `modules/InternalOracle.sol`, `modules/Admin.sol`, `modules/Base.sol` | Import `Ownable` only for `Ownable.Unauthorized` error selector. No `is Ownable` inheritance, no `_initializeOwner`, no `Ownable._owner` slot collision. | ✓ Error-only import, no parallel auth. |

### 3.3 Sweep -hardcoded `msg.sender ==` against constants

Regex: `msg\.sender\s*==` across `dex/evm/src/`.

Found 0 hits in modules. In sibling contracts:
- `PoolProxy.sol:38,77` -`msg.sender != DEPLOYER` (proxy admin gate, immutable)
- `StakedToken.sol:41` -`msg.sender != STAKING` (immutable role)
- `SoulboundToken.sol:31,36` -`msg.sender != minter` (storage role, governed via own setter)

None of these affect the pool governance surface; all are intra-contract role gates
on dedicated single-purpose contracts.

**Conclusion: zero residual governance leaks.**

---

## 4. Per-category scores

### Architect

| Category | Score | Rationale |
|---|---|---|
| **Design** | **9.7** | Path α surface is now closed: 1 `_owner()` fn, 6 call sites, 1 fall-through. `IPool.PoolStorage.ac` append-only. Sibling-contract `Ownable` instances are correctly scoped to non-pool surfaces. Diamond/module separation crisp. |
| **Logic** | **9.7** | All 6 module governance gates traverse `Base._owner()`. External `owner()` view now consistent with internal gate. `address(this)` allowance in `InternalOracle.updateFeed` is intentional self-call path, justified. PathAlphaACTest covers atomic rotation, gate, reversibility, storage layout. **Was 9.4 in Round 3 -now resolved.** |

### Security

| Category | Score | Rationale |
|---|---|---|
| **Governance** | **9.7** | Zero raw `_s().owner` reads outside the fall-through. Zero hardcoded address checks in pool surface. Zero parallel `Ownable` inheritance on pool modules (error-selector imports only). Atomic N-pool rotation in 1 tx via singleton AC verified. **Was 9.4 in Round 3 -now resolved.** |
| **Ecosystem** | **9.6** | alm consumes peripheral PP cleanly (`alm/evm/test/AlmCommon.t.sol:6,68-69`, `alm/evm/script/Deploy.s.sol:29,131-136`). dex consumes peripheral AC + PP. No circular deps. Single source for ABIs preserved. |

### Conventions

| Category | Score | Rationale |
|---|---|---|
| **Optimization** | **9.6** | `_owner()` is `view`, hot-path is single SLOAD when `ac == 0` (legacy). Singleton path adds 1 staticcall -acceptable for governance ops. No layout regressions; storage append-only verified by test. |
| **Conciseness** | **9.5** | 2-LOC remediation. No DRY rework executed (Stage 3f no-op verdict held). Module call sites read uniformly. `Pool.owner()` view is a 1-liner delegating to `_owner()`. |

---

## 5. Pass/Fail vs 9.5+ gate

| Category | Score | ≥ 9.5 |
|---|---|---|
| Design | 9.7 | ✓ |
| Logic | 9.7 | ✓ |
| Governance | 9.7 | ✓ |
| Ecosystem | 9.6 | ✓ |
| Optimization | 9.6 | ✓ |
| Conciseness | 9.5 | ✓ |

**Round 4 result: PASS (6/6 ≥ 9.5).**

---

## 6. Golden-rule sequential-pair determination

The golden rule requires **two sequential rounds** of 9.5+ in every category.

### Interpretation

- **Round 3** = FAIL (Logic 9.4, Governance 9.4 -surfaced 2 residual `_s().owner` leaks).
- **`9e3670f`** = 2-LOC remediation patch (not itself a scored round; a fix between rounds).
- **Round 4** = PASS (this document).

Per a strict reading of "two sequential 9.5+ rounds", Round 4 is the **first** of the
required pair. The remediation patch reset the sequence -Round 3's failure invalidates
any prior near-pass, and the new sequence starts at Round 4.

**Recommendation: posture for golden rule satisfaction**

1. **Hold Path α surface frozen** -no further edits to `Base._owner`, `IPool.PoolStorage.ac`,
   `Admin.setAc/getAc`, `InternalOracle.sol:77`, or `Pool.sol:402` until Round 5 lands.
2. **Schedule Round 5** as a clean re-pass with no intervening commits to dex governance
   surface. Peripheral and alm work may proceed in parallel without blocking the pair.
3. **Pre-Round-5 self-check**: re-run the same 3 sweeps (raw `_s().owner` regex, parallel
   `Ownable` inheritance on modules, hardcoded `msg.sender ==` on pool surface). If all
   three remain at 0 leaks and the test suite remains 173/173, Round 5 should reproduce
   the Round 4 score profile.
4. **If Round 5 passes 6/6 ≥ 9.5** → golden rule satisfied → unlock Phase 42C security
   batch.
5. **If Round 5 fails any category** → sequence resets; remediate and start a new pair.

**Sequential pair status as of Round 4: 1 / 2 complete.** Round 5 is required.

---

## 7. Blockers / action list

None for Round 4 (passed). For golden-rule completion:

- [ ] Schedule Round 5 with no intervening dex-governance commits.
- [ ] Confirm `forge build && forge test` remains green @ HEAD before Round 5 audit.
- [ ] Run identical 3-sweep regression in Round 5 to confirm zero regression.
